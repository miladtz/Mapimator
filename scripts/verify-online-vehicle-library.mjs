import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises';
const [project,overlay,app]=await Promise.all(['../src/core/project.ts','../src/core/onlineProjectOverlays.ts','../src/app/App.tsx'].map((url)=>readFile(new URL(url,import.meta.url),'utf8')));
const icons=['sedan','suv','taxi','pickup','van','delivery-van','bus','coach','small-truck','box-truck','semi-truck','tanker-truck','motorcycle','passenger-plane','cargo-plane','private-jet','helicopter','container-ship','cargo-vessel','bulk-carrier','oil-tanker','lng-carrier','ferry','cruise-ship','small-boat','speedboat','yacht','sailboat','passenger-train','high-speed-train','commuter-train','metro','freight-train','dot','pulse','directional-capsule','arrow','package','person','money'];
for(const icon of icons) assert.match(project,new RegExp(`'${icon}'`),icon); assert.match(overlay,/Path2D/); assert.match(overlay,/routeVehicleImage/); assert.match(overlay,/vehicle\.bearing/); assert.match(app,/Vehicle Type/); assert.doesNotMatch(app,/selectedSegment\.appearance\?\.vehicleType/);
console.log(`Online Vehicle Library: ${icons.length} MapMotion-owned vector marker choices and timeline ownership passed.`);
